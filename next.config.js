/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'unsplash.com']
      },
    /* config options here */
  }
  
  module.exports = nextConfig